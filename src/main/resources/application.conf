postgres-config {
  n-threads = 32
  url = "jdbc:postgresql:social"
  user = "docker"
  pass = "docker"
}

ember-config {
  host = 0.0.0.0
  port = 4041
}

security-config {
  secret = "secret"
  jwt-expiry-duration = "1 day"
}